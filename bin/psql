#!/usr/bin/env bash
# Drop into a psql shell against the dev database via local socket.

set -euo pipefail

ROLE="${LOVELY_PG_ROLE:-lovely_rs}"
DB="${LOVELY_PG_DB:-lovely_rs}"

exec psql -U "$ROLE" -d "$DB" -h localhost "$@"
