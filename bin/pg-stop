#!/usr/bin/env bash
# Stop the Homebrew Postgres service. Data is preserved on disk
# (`/opt/homebrew/var/postgresql@17`) — `bin/pg` will pick it back up.

set -euo pipefail

SERVICE="${LOVELY_PG_SERVICE:-postgresql@17}"

echo "==> Stopping $SERVICE"
brew services stop "$SERVICE"
