# Placeholder Secret manifest. DO NOT commit real values.
# Prefer creating this imperatively or via a sealed/external-secrets controller:
#
#   kubectl create secret generic lovely-secrets \
#     --from-literal=SESSION_SECRET="$(openssl rand -hex 32)" \
#     --from-literal=GITHUB_CLIENT_SECRET="..." \
#     --from-literal=GOOGLE_CLIENT_SECRET="..." \
#     --from-file=APPLE_PRIVATE_KEY=AuthKey_XXXXXXXXXX.p8 \
#     --from-literal=DB_PASSWORD="..." \
#     --from-literal=DATABASE_URL="postgres://lovely:PASS@postgres:5432/lovely"
apiVersion: v1
kind: Secret
metadata:
  name: lovely-secrets
  labels:
    app: lovely-server
type: Opaque
stringData:
  SESSION_SECRET: "REPLACE_ME_64_HEX_BYTES"
  GITHUB_CLIENT_SECRET: "REPLACE_ME_GITHUB_OAUTH_CLIENT_SECRET"
  GOOGLE_CLIENT_SECRET: "REPLACE_ME_GOOGLE_OAUTH_CLIENT_SECRET"
  APPLE_PRIVATE_KEY: |
    -----BEGIN PRIVATE KEY-----
    REPLACE_ME_WITH_CONTENTS_OF_AuthKey_XXXXXXXXXX_DOT_P8
    -----END PRIVATE KEY-----
  DB_PASSWORD: "REPLACE_ME_DB_PASSWORD"
  DATABASE_URL: "postgres://lovely:REPLACE_ME_DB_PASSWORD@postgres:5432/lovely"
